
-- Crear política permisiva para el bucket tigreimg
CREATE POLICY "Allow all operations on tigreimg" 
ON storage.objects
FOR ALL
USING (bucket_id = 'tigreimg')
WITH CHECK (bucket_id = 'tigreimg');


-- Ver si el bucket tigreimg existe
SELECT * FROM storage.buckets WHERE id = 'tigreimg';

-- Crear el bucket si no existe
INSERT INTO storage.buckets (id, name, public)
VALUES ('tigreimg', 'tigreimg', true)
ON CONFLICT (id) DO NOTHING;